apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "babelfish.fullname" . }}-test-connection"
  labels:
    {{- include "babelfish.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "babelfish.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
